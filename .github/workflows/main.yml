on:
  pull_request:
  push:

jobs:
  main:
    permissions:
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: foo
        uses: ./
        with:
          local-image: 'ghcr.io/mozilla/neqo-qns:latest'
